import os

# Create the main folder
os.mkdir("subfolder")
print("subfolder created")

# Create a subfolder inside the main folder
os.mkdir("subfolder/subfolder1")
print("subfolder1 created inside subfolder")
